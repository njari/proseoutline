# ---------------------------------------------------------------------------
# ProseOutline design system — single source of truth for all colours.
# Every colour used in the UI must be defined here with its semantic role.
# ---------------------------------------------------------------------------

# Brand
PRIMARY  = "#1e3a8a"   # Navy  — primary buttons, headings, key UI chrome
ACCENT   = "#0ea5e9"   # Sky   — highlights, hover states, spinner

# Surfaces
SURFACE  = "#ffffff"   # White — card and editor backgrounds
BG_PAGE  = "#f0f9ff"   # Sky extra-light — full-page background
BG_PANEL = "#e0f2fe"   # Sky light — sidebar and right-panel backgrounds

# Borders
BORDER   = "#bae6fd"   # Sky medium — all dividers and outline borders

# Text
TEXT_BODY   = "#334155"   # Slate 700 — primary readable content
TEXT_MUTED  = "#64748b"   # Slate 500 — secondary / helper text
TEXT_SUBTLE = "#94a3b8"   # Slate 400 — disabled / empty-state labels

# Semantic status
SUCCESS  = "#059669"   # Emerald 600 — positive confirmations
WARNING  = "#f59e0b"   # Amber 400   — cautionary messages
ERROR    = "#ef4444"   # Red 500     — validation errors and failures
